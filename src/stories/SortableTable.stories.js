import React from "react";

import { SortableTable } from "../components/SortableTable";

export default {
	title: "Sortable Table Component",
	component: SortableTable,
};

const Template = () => <SortableTable />;

export const App = Template.bind({});
